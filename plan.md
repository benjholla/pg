1. **Remove `EphemeralGuardrails`**: We already deleted the file and usages, so we don't strictly need to do this again.
2. **Implement `validateAndWrap` in `EphemeralGraph`**: Add a package-private method that enforces universe lineage and wraps `UniverseNode` into `ShadowUniverseNode`. Ensure `addNode` and `addEdge` use this to enforce the active firewall.
3. **Add `tombstonedNodeIds` and `tombstonedEdgeIds` `BitSet`s** in `EphemeralGraph`, and use them in `removeNode` and `removeEdge`. Remove the strict `instanceof EphemeralNode` checks to allow removals of `ShadowUniverseNode`s and `ShadowEdge`s by adding them to these tombstones.
4. **Implement `ShadowEdge`**: Create a unified wrapper for edges (`UniverseEdge` or `EphemeralEdge`). It lazily shields endpoints by calling `transaction.validateAndWrap`. Use `ShadowTagSet` and `ShadowAttributeMap` for `UniverseEdge`s, and return native attributes/tags for `EphemeralEdge`s.
5. **Implement `ShadowNodeIterator` and `ShadowEdgeIterator`**: These iterators intercept items from the core engine and lazily shield them.
6. **Implement `ShadowNodeSet` and `ShadowEdgeSet`**: These are composite sets combining the backing set (from the core engine), the local additions (`localAdds`), and filtering out items present in the tombstone sets. They must implement all methods of `NodeSet`/`EdgeSet`, handling composite math properly.
7. **Modify `EphemeralGraph` to use Shadow Sets**: Update methods like `nodes()`, `edges()`, traversals (`forward()`, `reverse()`, etc.) to return appropriate `ShadowNodeSet`/`ShadowEdgeSet` instances, merging the base engine results with local transactional states.
8. **Pre-commit Instructions**: Run the pre-commit instructions as usual to check for verification, review, and reflection.
