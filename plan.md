1. Add new invariant tests to test behaviors of missing edge cases.
   - `DifferenceNodeInvariantTest` which asserts `difference(Node)` properties.
   - `DifferenceEdgeInvariantTest` which asserts `difference(Edge)` properties.
   - `DifferenceEdgesEdgeInvariantTest` which asserts `differenceEdges(Edge)` properties.
2. Complete pre-commit step
